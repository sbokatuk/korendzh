@page
@model AcceptInviteModel
@{
    ViewData["Title"] = "Принять приглашение";
}
<div class="card" style="max-width:420px;margin:1rem auto;">
    <h1>Установить пароль</h1>
    <p>Введите пароль для входа в Korendzh.</p>

    @if (Model.Failed)
    {
        <div class="error">
            Ссылка-приглашение недействительна или истекла. Попросите администратора отправить новое.
        </div>
    }
    else
    {
        <form method="post">
            <input type="hidden" asp-for="Token" />
            <div asp-validation-summary="ModelOnly" class="error"></div>
            <div class="field">
                <label asp-for="Input.Password">Новый пароль</label>
                <input asp-for="Input.Password" type="password" autocomplete="new-password" required />
                <span asp-validation-for="Input.Password"></span>
            </div>
            <div class="field">
                <label asp-for="Input.ConfirmPassword">Подтверждение</label>
                <input asp-for="Input.ConfirmPassword" type="password" autocomplete="new-password" required />
                <span asp-validation-for="Input.ConfirmPassword"></span>
            </div>
            <button type="submit" class="btn">Установить пароль и войти</button>
        </form>
    }
</div>
